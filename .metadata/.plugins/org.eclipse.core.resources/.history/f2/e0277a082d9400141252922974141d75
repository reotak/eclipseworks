package gui;

import java.lang.reflect.Type;

abstract class SetArgumentsWindowManager implements CatchArguments {
	public SetArgumentsWindowManager(Type[] argTypes) {
		SetArgumentsWindow window = new SetArgumentsWindow(argTypes, this);
		window.setVisible(true);
	}

	abstract public void catchArguments(Object[] args);
}
